# Cover Example

This is a complete Dart package demonstrating how to use the `cover` package programmatically.

## How to run

From this directory:

```bash
dart pub get
dart run bin/cover_example.dart
```

This will analyze the sample coverage file in `params/lcov.info`.
